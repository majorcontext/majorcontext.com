export { SITE_URL, MAIN_CATEGORIES, OPTIONAL_CATEGORIES } from './llms-shared';

export const INTRO = `Keep is a policy engine for AI agent tool calls. It defines declarative YAML rules \
that deny, redact, or log structured API calls before they reach upstream services. Ships \
as a Go library, an MCP relay for Claude Code, and an HTTP LLM gateway. Rules use CEL \
(Common Expression Language) for matching. Open source, actively developed.`;
